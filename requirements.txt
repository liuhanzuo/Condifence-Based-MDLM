# 核心依赖（Python 3.11, 单卡 CUDA）
# torch 需按 GPU 选择正确 CUDA 版本：
#   - Blackwell(5090, 驱动新): pip install torch --index-url https://download.pytorch.org/whl/cu128
#   - H100(驱动550, 仅支持CUDA≤12.4): 官方 cu124 索引 torch 最高只到 2.6.0，必须先单独装：
#       pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#     ⚠️ 切勿在 H100 上直接 `pip install -r requirements.txt`——下行的 torch>=2.10 会把
#        cu124 的 2.6.0 升级成 cu126/cu128 的新版，CUDA 初始化会失败。先装好 torch 再装其余包。
torch>=2.6   # H100 实测 2.6.0+cu124 可用；5090 用 2.10+/cu128
transformers>=4.44,<5
accelerate
safetensors
sentencepiece
peft
scikit-learn
matplotlib
numpy
huggingface_hub
pandas
pyarrow          # 读取 GSM8K/WikiText 的 parquet 数据文件
# 仅 PDF 导出用：
markdown
playwright
